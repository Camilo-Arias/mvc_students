CREATE TABLE IF NOT EXISTS estudiantes " + "(NAME TEXT NOT NULL, LASTNAME TEXT NOT NULL, DATEBIRTH TEXT NOT NULL, EMAILINS TEXT PRIMARY KEY NOT NULL, EMAILPER TEXT NOT NULL, NUMBERPHONE INTEGER NOT NULL, NUMBERHOME INTEGER NOT NULL, CAREER TEXT NOT NULL );
